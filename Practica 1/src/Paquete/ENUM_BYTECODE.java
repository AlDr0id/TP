package Paquete;

/** Tipo enumerado que representa los posibles ByteCodes que se pueden introducir al programa */
public enum ENUM_BYTECODE {	

	PUSH,
	LOAD,
	STORE,
	ADD,
	SUB,
	MUL,
	DIV,
	OUT,
	HALT;
}